const items = [{x:5111,y:6111}, 'item2', 'item3'];
const copy = [];

// before
for (let i=0; i<items.length; i++) {
  copy.push(items[i]);
  x= items.item1;
  
  x.toFixed(3);
}
console.log(copy);

/*
const pointsFormattedJ = [];
for (TODO; TODO; TODO) {
  pointsFormattedJ[i] = `${TODO},${TODO}`;
}
const points = pointsFormattedJ.TODO;
console.log(points);
*/






